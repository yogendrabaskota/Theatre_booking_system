<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Choose Seats</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>

<body class="bg-secondary">
  <input type="hidden" value="<%-uid %> " id="uid" name="uid" required>
  <main>
    <header>
      <!-- Fixed navbar -->
      <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="#"><%-uid %> </a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse"
            aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
            </ul>
            <div class="text-end">
              <a href="logout">
                <button type="button" class="btn btn-outline-light me-2">
                  logout
                </button></a>
            </div>
          </div>
        </div>
      </nav>
    </header>
    <br></br>
    <div class="container py-5 h-100">
      <div class="row d-flex justify-content-center align-items-center h-100">
        <div class="col-12 col-md-8 col-lg-6 col-xl-5">
          <div class="card bg-dark text-white" style="border-radius: 1rem">
            <div class="card-body p-5 text-center">
              <h2 class="mb-2 text-uppercase me-2">Choose Seat</h2>
                <form action="/customer/seats" method="POST">

                          <input type="hidden" value="<%-uid %> " id="uid" name="uid" required>
                          <input type="hidden" value="<%-date %> " id="date" name="date" required>
                          <input type="hidden" value="<%-mid %> " id="mid" name="mid" required>
                          <input type="hidden" value="<%-snum %> " id="snum" name="snum" required>
                          <input type="hidden" value="<%-amount %> " id="amount" name="amount" required>
                  <div class="form-outline form-white mb-4">
                  <label for="mid" class="form-label">movie name:</label>
                  <input type="text" value="<%-mname %> " id="mname" name="mname" class="form-control form-control-lg" required>
                </div>
                <div class="form-outline form-white mb-4">
                  <label for="time" class="form-label">screening time :</label>
                    <input type="text" id="time" value="<%-time %> " name="time" class="form-control form-control-lg" required>
                  
                </div>
                <div class="form-outline form-white mb-4">
                </select>
                  <label for="seats" class="form-label">seats:</label>
                  <% if (snum == 1) { %> 
                  <select id="seats" name="seats" class="form-control form-control-lg">
                    <% if ( seats.length> 0){ %>
                      <% for ( let i=0 ; i < seats.length ; i++ ) { %>
                        <option value=" <%-seats[i].sid%> ">
                          <%-seats[i].sid%>
                        </option>
                        <% } %>
                          <% } else { %>
                            <h2> data not found </h2>
                            <% } %>
                  </select>
                  <% } else { %>
                        <select id="seats" name="seats" multiple class="form-control form-control-lg">
                          <% if ( seats.length> 0){ %>
                            <% for ( let i=0 ; i < seats.length ; i++ ) { %>
                              <option value="<%-seats[i].sid%> ">
                                <%-seats[i].sid%>
                              </option>
                              <% } %>
                                <% } else { %>
                                  <h2> data not found </h2>
                                  <% } %>
                        </select>
                <% } %>
              </div>
              <div class="form-outline form-white mb-4">
                  <input type="submit" value="Select seat" class="btn btn-sm btn-success">
                </div>
                </form>
            </div>
          </div>
        </div>
      </div>
      
</body>

</html>